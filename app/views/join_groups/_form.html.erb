<%= form_for(@join_group) do |f| %>
  <% if @join_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@join_group.errors.count, "error") %> prohibited this join_group from being saved:</h2>

      <ul>
      <% @join_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br />
    <%= f.number_field :group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
